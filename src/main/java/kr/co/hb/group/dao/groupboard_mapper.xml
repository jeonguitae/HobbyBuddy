<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC	"-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="kr.co.hb.group.dao.GroupBoardDAO">

	<select id="list" resultType="kr.co.hb.group.dto.GroupBoardDTO">
		SELECT * FROM gboardlist ORDER BY gidx DESC
	</select>
	
	<insert id="gwrite" parameterType="HashMap">
		INSERT INTO gboardlist(id, subject, content, bhobby, mhobby, minage, maxage, gender, city, area, meeting_date, maxmem, mannertp)
		VALUES(#{id}, #{subject}, #{content}, #{bhobby}, #{mhobby}, #{minage}, #{maxage}, #{gender}, #{city}, #{area}, #{meeting_date}, #{maxmem}, #{mannertp})
	</insert>
	
	<select id="gdetail" resultType="kr.co.hb.group.dto.GroupBoardDTO">
		SELECT * FROM gboardlist
		WHERE gidx = #{id}
	</select>
	
	<select id="gupdate" resultType="kr.co.hb.group.dto.GroupBoardDTO">
		SELECT * FROM gboardlist
		WHERE gidx = #{id}
	</select>
	
	<update id="gupdate"> 
		UPDATE gboardlist SET subject = ${subject}, 
	</update>
</mapper>